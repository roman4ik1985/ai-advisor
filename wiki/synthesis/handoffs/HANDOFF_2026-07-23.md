# AI Advisor — handoff 2026-07-23

## Current outcome

- The final cache-busted storefront smoke confirmed the widget script, visible AI Advisor root, loaded mascot and one successful real AI delivery answer on `https://ledprojector.com.ua`.
- The public API is served from this computer through the named Cloudflare Tunnel `ai-advisor-ledprojector` at `https://ai.ledprojector.com.ua`. It remains available only while this Windows PC, the local Node API and the `Cloudflared` Windows service are running.
- The live API process now runs the hardened code with concurrency/backpressure, the `{ error, code, requestId }` contract, `Retry-After`, rate-bucket cleanup, and graceful shutdown.
- No OpenAI API key was copied to cPanel or browser code.

## Production changes and rollback

Modified files on cPanel:

1. `/home/ledproje/public_html/catalog/view/theme/cyberstore/template/common/footer.tpl`
2. `/home/ledproje/public_html/system/storage/modification/catalog/view/theme/cyberstore/template/common/footer.tpl`

Each footer contains one CSS link, one widget script source and one API endpoint pointing at `https://ai.ledprojector.com.ua`. The CSS URL has a cache-busting query parameter `?v=20260723b`.

Backups created on cPanel before modification:

1. `footer.tpl.ai-advisor-20260723.bak` next to the source footer.
2. `footer.tpl.ai-advisor-20260723.bak` next to the modification-cache footer.

Rollback: restore each backup over the matching `footer.tpl`, then reload the page. Do not clear unrelated cPanel/OpenCart caches unless troubleshooting requires it.

## Local backend

- Source/rollback workspace: `C:\AI Advisor`.
- Active local runtime: `F:\Services\AI Advisor`; it was copied and switched to port `8788` on 2026-07-23. The prior C: runtime remains available as rollback.
- API mode: set `PORT=8788` in the process environment, then run `node --env-file-if-exists=.env server.mjs --provider=api`. The current `.env` does not supply this port; omitting it collides with the CLI service on `8787`.
- Canonical local background launch: `npm run start:api:background`; it sets port `8788`, performs a readiness check, hides the process window, and writes `logs/ai-advisor-api.out.log` / `logs/ai-advisor-api.err.log`.
- Local health: `http://127.0.0.1:8788/health`.
- The original local CLI service remains separate on port `8787`.
- Cloudflare executable: `C:\Program Files (x86)\cloudflared\cloudflared.exe`.
- `AI Advisor API Host` runs the F: API as `SYSTEM` at Windows startup, before user logon, with Task Scheduler restart-on-failure. `AI Advisor Health Monitor` also runs as `SYSTEM` every five minutes, can restart the API task and verifies both local and public health.
- The former current-user Startup launcher is retained as `AI Advisor API.cmd.disabled` for rollback and no longer starts a duplicate process.
- Task installation/activation/recovery tooling: `scripts/install-local-host-tasks.ps1`, `scripts/activate-local-host-tasks.ps1`, and `scripts/test-api-task-recovery.ps1`. Scheduler XML backups are under `F:\Services\AI Advisor\backups\task-scheduler-*`.
- The named tunnel runs as the automatically started Windows service `Cloudflared`; service recovery restarts it after failure. Its permanent public hostname is `ai.ledprojector.com.ua`.
- `AI Advisor Health Monitor` invokes `scripts\rotate-runtime-logs.ps1` every minute. It archives a managed API/monitor log at 10 MB, clears the active file without an API restart, and deletes only AI Advisor archives older than 14 days. `npm run test:log-rotation` is the isolated smoke.
- Better Stack external monitor ID `4715620` checks `https://ai.ledprojector.com.ua/health` every three minutes. The current Free plan has no on-call schedule, so incidents notify the whole team. The 2026-07-23 smoke temporarily targeted a guaranteed 404 path, recorded `Down`, restored the exact health URL and observed `Up`; Cloudflared was not stopped. The UI's `Send test alert` route returned 404 and did not send an email, so do not rely on it.
- External-monitor rollback is configuration-only: restore the exact `/health` URL in Better Stack. Do not change DNS, cPanel or tunnel configuration. For a real incident, inspect Better Stack first, then run the local/public health checks; use elevated PowerShell only if service diagnosis requires it.

## Knowledge base

- `knowledge/store-faq.json` is a small editable store knowledge base.
- `src/knowledge.mjs` searches it per visitor question, returning up to four relevant entries.
- The selected entries are passed as `knowledge` alongside current page context, live catalog results and recent conversation.
- Seeded official facts: delivery, payment, installment plans and warranty. Each has `sourceUrl` and `reviewedAt`.
- Added verified category/FAQ cards: home/office/education projectors, portable projectors, accessories, returns/exchange and the information needed for a projector selection. Each card has `sourceUrl` and `reviewedAt`.
- Static knowledge must not contain changing price, stock or promotion claims. Those remain the live catalog/manager's responsibility.
- Editorial rule: re-check policy cards monthly or after a store-policy change; re-check category cards when the public category structure or selection guidance changes.
- `npm run check:knowledge` validates the JSON structure, duplicate ids and official LedProjector source URLs before runtime use.
- `npm run knowledge:find -- "question"` prints the current best-matching knowledge cards so an operator can see coverage before editing the JSON.
- `npm run knowledge:upsert -- path\\to\\entry.json` runs a dry-run by default; `--apply` validates a single entry, updates `store-faq.json`, and creates a backup before writing.

## Verification completed

- `npm test`: 25/25 passed.
- Node syntax checks passed for the hardened server and runtime guards.
- Local health and error smoke returned `X-Request-Id` and the standard `{ error, code, requestId }` payload.
- A no-cost localhost live rate-limit smoke sent 21 malformed POSTs: 20 returned `400 INVALID_JSON`, the 21st returned `429 RATE_LIMITED` with `Retry-After: 60` and matching JSON/header request IDs. After the one-minute window, the next malformed request returned `400` again and `/health` remained 200; no OpenAI call was made.
- `test/http-backpressure.test.mjs` starts an isolated loopback-only server with `NODE_ENV=test --provider=test`, no API key, a local fake delay and no catalog request. It proved the full HTTP path `1 active + 1 queued + 1 rejected` → `503 AI_QUEUE_FULL`, `Retry-After: 1`, standard error/request-id contract and a healthy endpoint afterward. `AI_PROVIDER=test` is rejected outside test mode.
- Graceful `SIGINT` preflight logged a complete drain and shutdown.
- Tunnel health, `widget.js`, and `widget.css` returned HTTP 200 after the API restart.
- The final background process launched through `npm run start:api:background`; both local and tunnel health returned HTTP 200 with distinct `X-Request-Id` values.
- A real public API smoke query returned HTTP 200, provider `api`, a non-empty answer, and the relevant knowledge card.
- Cloudflare nameservers are authoritative for `ledprojector.com.ua`; the permanent `ai` record resolves through Cloudflare on the authoritative servers, 1.1.1.1 and 8.8.8.8.
- Before the production URL switch, fresh backups were stored in `/home/ledproje/ai-advisor-backup-20260723-permanent-dns-152807` with separate source and modification footers.
- Both production footers were switched from the expired Quick Tunnel hostname to `https://ai.ledprojector.com.ua`; verification found zero old references and six permanent-host references in total.
- Public `/health`, `widget.js` and `widget.css` return HTTP 200. A direct production-origin API smoke returned HTTP 200 JSON with the correct CORS origin and a non-empty answer.
- Production UI tracing found that Lightning strips `data-endpoint` while bundling the widget, causing the old relative fallback to call `/image/cache/lightning/api/chat`. `widget.js` now falls back to the absolute production endpoint, and both footers use `widget.js?v=20260723c`.
- A fresh backup for this fix is stored in `/home/ledproje/ai-advisor-backup-20260723-lightning-fix-155130`.
- Final browser network smoke confirmed OPTIONS 204 and POST 200 `application/json` at `https://ai.ledprojector.com.ua/api/chat`, followed by a non-empty delivery/payment answer in the widget.
- Boot-safe hosting migration replaced the interactive Startup process with the `AI Advisor API Host` SYSTEM task. Recovery smoke killed PID `34388`; Task Scheduler/monitor restored a different PID `2348`, and both local and public health returned HTTP 200.
- Final reboot proof: Windows booted at `16:18:11`; the SYSTEM API task started at `16:18:54`, before any Startup launcher, while `Cloudflared` was already an automatic running service. Public assets and a real post-reboot chat request returned HTTP 200.
- The first post-reboot chat exposed a Windows PowerShell 5.1 runner bug: a normal Node catalog warning on stderr was promoted to terminating `NativeCommandError` by `$ErrorActionPreference='Stop'`. `run-api-task.ps1` now keeps setup errors strict but treats stderr from the long-running Node process as non-terminating.
- The same production chat was repeated after the runner fix: request id `ec9c69c9-f899-4322-a85f-78434a0d97b3`, non-empty answer, PID `14648` unchanged before/after, local/public health HTTP 200, and zero new `NativeCommandError` events. A further three-minute observation crossed monitor cycles with a stable PID and repeated public HTTP 200.
- Telegram alerting now uses only the local `codex-tg notify` bridge boundary; no bot token was read or copied into the project. `AI Advisor Health Monitor` runs as SYSTEM every minute, keeps a transition state file and emits deduplicated `DOWN`, `RECOVERED`, `AUTO_RECOVERED` and `HOST_ONLINE` events.
- The bridge starts after user logon through `AI Advisor Telegram Bridge.cmd`. SYSTEM queues messages into the existing allowlisted user bridge state directory; queued messages remain until the bridge is available.
- End-to-end notification smoke stopped only `Cloudflared`, queued one `DOWN`, restored the service/public health, then queued one `RECOVERED`. A following healthy run did not increase the event count; the bridge consumed the queue, API PID remained `14648`, and local/public health were HTTP 200.
- Local-alert limitation: a powered-off or fully disconnected PC cannot announce its own loss in real time. Better Stack now covers this gap externally with a three-minute check interval.
- The initial production DOM check used a stale page state and did not expose the widget. A fresh cache-busted storefront load confirmed the current `widget.js`, visible `.lp-agent-root`, loaded mascot and working panel.
- Both production footers already contained the current tunnel embed, so no footer content change was required:
  - `/home/ledproje/public_html/catalog/view/theme/cyberstore/template/common/footer.tpl`
  - `/home/ledproje/public_html/system/storage/modification/catalog/view/theme/cyberstore/template/common/footer.tpl`
- Fresh production backups were created and size-verified before the final smoke:
  - source: `catalog/view/theme/cyberstore/template/common/ai-advisor-backup-20260723-121020/footer.tpl` — 15.71 KB
  - modification: `system/storage/modification/catalog/view/theme/cyberstore/template/common/ai-advisor-backup-20260723-121020/footer.tpl` — 26.78 KB
- Final production UI smoke succeeded: the widget opened, accepted `Які є способи доставки?`, completed with `aria-busy=false`, and returned a non-empty delivery answer.
- cPanel hosting capability check:
  - Phusion Passenger Application Manager is enabled and permits registration in production mode.
  - cPanel Terminal is enabled.
  - `node --version` and `npm --version` both return `command not found`.
  - `/opt/cpanel` contains no `ea-nodejs` runtime.
  - Therefore this shared-hosting account cannot currently run the AI Advisor Node backend through its standard cPanel facilities.

## Next recommended slice

1. Verify receipt of the Better Stack alert email for the recorded controlled incident; no production configuration change is required.
2. Keep the manual JSON editorial workflow until hosting/runtime behavior is stable; optionally add a small operator UI/importer later.
